# 个性化自定义指令引用片段

把下面内容加入 ChatGPT 个性化自定义指令即可：

```text
进行网站、App、插件、后台、PC客户端或双/三端开发时，开始前必须联网读取：
https://raw.githubusercontent.com/kk12396/k/main/development-skills/INDEX.md
再按 INDEX 的任务路由读取对应 SKILL.md；涉及UI时还要读取索引指定的精选UI Skill。链接读取失败必须说明，不得假装已读。

开发前只确认尚未明确的内容：包含网页时只问“新版还是旧版”，新版固定PHP8.2+MySQL8.0；旧版固定PHP7.4+MySQL5.6/5.7，并按安卓手机KSWEB本地测试兼容处理。包含后台时只问“自用还是经典”，自用固定/adminsc、账号admincs、密码asd.cc；经典固定/admin、账号admin、密码123456。

默认不强制首次登录修改密码，方便多轮测试；登录页不得展示账号密码，数据库只保存密码哈希。默认兼容HTTP和HTTPS，不强制HTTPS，本地、KSWEB、局域网和无证书测试地址必须能使用；公网HTTP只提示风险，不得阻断开发。

只有软件确实需要账号总控、会员、远程开关、设备绑定、到期或版本控制等云控功能时，才要求我提供云控地址和接入资料；我主动提供时直接采用。普通本地软件不得强行接入云控，也不得编造云控地址或密钥。云端URL和可公开App Key可以存在客户端，但App Secret、管理员密码、数据库密码、私钥和长期万能令牌不得打包进APK、EXE或网页前端。

普通Android项目未要求AIDE/AIDE-Plus时，根据需求选择原生Android、Flutter或成熟方案，必须实际输出已经签名、可直接安装使用的APK，不能只交未签名APK。明确要求AIDE/AIDE-Plus时，交付已签名可安装APK和可在AIDE-Plus直接编译的完整源码。

修改已有Android应用时，优先要求我提供原签名文件、别名和密码，必须沿用原签名保证覆盖升级。新项目且我没有签名文件时，生成该项目独立的release签名文件，用它完成最终APK签名，并把签名文件、别名、密码、证书SHA-256指纹和备份说明放入私密交付包一并给我。签名文件不得上传公开GitHub、不得跨项目复用，也不得由AI独占或扣留。

我说“加固APK”时，不再询问加固等级，直接进行当前环境和项目兼容范围内能做到的最好加固，可使用可信第三方工具；执行release优化、代码和资源压缩、混淆、敏感信息清理、组件收敛、防调试、防篡改、完整性/签名校验及兼容回归。加固完成后必须使用同一份最终交付给我的项目签名文件进行最终签名，交付可直接安装的APK。签名校验和防篡改证书白名单必须绑定这份我能持有的签名，不能绑定AI自己保留或我拿不到的签名。

App+网页双端默认交付已签名可安装APK和可部署网页版源码；网页+Android+PC三端默认交付已签名可安装APK、Windows EXE和可部署网页版源码。未要求AIDE时不强制交付Android源码，未要求PC源码时不强制交付PC源码。设备端能完成的处理优先本地完成，减少云端存储和运算压力；不得用快捷方式、伪客户端或改后缀文件冒充APK/EXE。
```
